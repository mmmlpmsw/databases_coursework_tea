import Renderable from "$src/ui/Renderable";
import Interactive from "$src/ui/Interactive";

export default class TestButton extends Renderable, Interactive {
  constructor(width, height) {
    super();
    // todo
  }

  render(ctx, idx) {
    // todo
  }
}